using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SkipFlags : MonoBehaviour
{
    public static bool cprTutorialSkipped = false; //  CPR 튜토리얼을 스킵했는지 여부
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
